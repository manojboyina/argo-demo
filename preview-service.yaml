apiVersion: v1
kind: Service
metadata:
  name: echo-preview
spec:
  selector:
    app: echo
  ports:
  - port: 80
    targetPort: 5678
  type: NodePortgit add .
git commit -m "initial blue green rollout"
git push